// TODO: Autogenerate from webpack.config.constants.json .EXTERNALS:
import ReactDOM from 'react-dom';
import React from 'react';

(function(window){
    window.React = React;
    window.ReactDOM = ReactDOM;
})(typeof window !== 'undefined' ? window : global);
